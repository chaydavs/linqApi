---
description: Quick code review of recently changed files
---

Use the code-reviewer agent to review all files modified in the current git working tree (staged and unstaged). Run `git diff --name-only` to find them. Focus on critical issues only. Be brief.
